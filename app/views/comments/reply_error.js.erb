<%# 返信の保存に失敗した場合に呼ばれる %>

<%# 何かFLASHが出ていた場合はそれをリセット %>
$("#comment_index_notice").html('')

<%# editでエラーが出ていた場合はそれをリセット %>
$("#edit_error_area").html('');

<%# form上のエラー用エリアにエラー用ファイルをレンダー（遷移ない為必要） %>
$("#reply_error_area").html("<%= j(render 'comments/comment_error', { comment: @comment }) %>")

console.log("reply_errorを通りました")
